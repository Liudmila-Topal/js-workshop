/**
 * Pseudocode for checking if a number is even
 * ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 * START
 *   INPUT number
 *   IF number % 2 = 0 THEN
 *     PRINT "The number is even"
 *   END IF
 * END
 */


if (number % 2 === 0) {
    console.log('The number is even.')
}